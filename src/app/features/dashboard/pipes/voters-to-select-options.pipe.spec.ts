import { VotersToSelectOptionsPipe } from './voters-to-select-options.pipe';

describe('VotersToSelectOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new VotersToSelectOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
