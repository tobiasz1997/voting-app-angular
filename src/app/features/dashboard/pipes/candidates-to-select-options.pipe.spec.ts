import { CandidatesToSelectOptionsPipe } from './candidates-to-select-options.pipe';

describe('CandidatesToSelectOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidatesToSelectOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
